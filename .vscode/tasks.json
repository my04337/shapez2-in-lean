{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "lake build",
            "type": "shell",
            "command": "lake build",
            "options": {
                "env": {
                    "PATH": "${env:USERPROFILE}\\.elan\\bin;${env:PATH}"
                }
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": []
        }
    ]
}
