# S2IL/Operations/Gravity/CommExt/CommNe.lean

- Lines: 270
- Declarations: 7 (theorems/lemmas: 7, defs/abbrev: 0, private: 2, sorry: 0)

## Landmarks

L6     namespace Gravity

## Declarations

```
L15    theorem    Gravity.isOccupied_placeQuarter_ne_layer : (obs : List Layer) (lay lay' : Nat) (d : Direction) (q : Quarter) (hne : lay ≠ lay') (d' : Direction) : isOccupied (placeQuarter obs lay d q) lay' d' = isOc...
L34    theorem    Gravity.isOccupied_placeFU_of_ne : (s : Shape) (obs : List Layer) (u : FallingUnit) (drop : Nat) (lay : Nat) (dir : Direction) (h : ∀ p ∈ u.positions, p.dir = dir → p.layer - drop ≠ lay) : isOccupied...
L63    theorem    Gravity.landed_placeFU_of_ne[priv] : (s : Shape) (obs : List Layer) (u : FallingUnit) (drop : Nat) (positions : List QuarterPos) (d : Nat) (h : ∀ q ∈ positions, q.layer > d → ∀ p ∈ u.positions, q.d...
L95    theorem    Gravity.landingDistance_placeFU_gap : (s : Shape) (obs : List Layer) (u v : FallingUnit) (drop : Nat) (h_gap : ∀ p ∈ u.positions, ∀ q ∈ v.positions, p.dir = q.dir → p.layer ≠ q.layer ∧ ¬(LayerIndex.v...
L138   theorem    Gravity.placeFallingUnit_comm_of_layer_disjoint : (s : Shape) (obs : List Layer) (u v : FallingUnit) (du dv : Nat) (h : ∀ p ∈ u.positions, ∀ q ∈ v.positions, p.dir = q.dir → p.layer - du ≠ q.layer - ...
L209   theorem    Gravity.placedLayer_ne_of_gap[priv] : (obs : List Layer) (u v : FallingUnit) (p q : QuarterPos) (h_ml_u : u.minLayer ≤ 2) (h_same_ml : u.minLayer = v.minLayer) (hne_layer : p.layer ≠ q.layer) (h_no...
L233   theorem    Gravity.settleStep_comm_dir_gap : (s : Shape) (obs : List Layer) (u v : FallingUnit) (h_gap : ∀ p ∈ u.positions, ∀ q ∈ v.positions, p.dir = q.dir → p.layer ≠ q.layer ∧ ¬(LayerIndex.verticallyAdjacent...
```
