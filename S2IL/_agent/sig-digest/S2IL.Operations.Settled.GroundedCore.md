# S2IL/Operations/Settled/GroundedCore.lean

- Lines: 361
- Declarations: 13 (theorems/lemmas: 13, defs/abbrev: 0, private: 0, sorry: 0)

## Landmarks

L6     namespace Shape

## Declarations

```
L15    theorem    Shape.getQuarter_clearPositions_of_not_mem : (s : Shape) (fus : List QuarterPos) (p : QuarterPos) (h_valid : p.layer < s.layerCount) (h_nf : ¬ (fus.any (· == p) = true)) : p.getQuarter (s.clearPosi...
L25    theorem    Shape.layer_lt_of_isGroundingContact_left : (s : Shape) (p1 p2 : QuarterPos) (h : Gravity.isGroundingContact s p1 p2 = true) : p1.layer < s.layerCount
L36    theorem    Shape.layer_lt_of_isGroundingContact_right : (s : Shape) (p1 p2 : QuarterPos) (h : Gravity.isGroundingContact s p1 p2 = true) : p2.layer < s.layerCount
L49    theorem    Shape.isBondedInLayer_preserves_groundedness : (s : Shape) {a mid : QuarterPos} (h_a_grounded : a ∈ Gravity.groundedPositions s) (h_edge : Gravity.groundingEdge s a mid = true) : mid ∈ Gravity.grou...
L67    theorem    Shape.reachable_preserved_of_grounded : (s : Shape) (fus : List QuarterPos) (h_fus : ∀ q, (fus.any (· == q) = true) → q ∉ Gravity.groundedPositions s) {start target : QuarterPos} (h_reach : Generic...
L97    theorem    Shape.groundedSeed_mono : (s1 s2 : Shape) (h_lc : s1.layerCount ≤ s2.layerCount) (h_gq : ∀ p : QuarterPos, p.layer < s1.layerCount → (∃ q, p.getQuarter s1 = some q ∧ !q.isEmpty = true) → p.getQuart...
L135   theorem    Shape.isGroundingContact_mono : (s1 s2 : Shape) (h_gq : ∀ p : QuarterPos, p.layer < s1.layerCount → (∃ q, p.getQuarter s1 = some q ∧ !q.isEmpty = true) → p.getQuarter s2 = p.getQuarter s1) : ∀ a b,...
L164   theorem    Shape.groundingEdge_mono : (s1 s2 : Shape) (h_gq : ∀ p : QuarterPos, p.layer < s1.layerCount → (∃ q, p.getQuarter s1 = some q ∧ !q.isEmpty = true) → p.getQuarter s2 = p.getQuarter s1) : ∀ a b, Grav...
L193   theorem    Shape.groundedPositions_mono : (s1 s2 : Shape) (h_lc : s1.layerCount ≤ s2.layerCount) (h_gq : ∀ p : QuarterPos, p.layer < s1.layerCount → (∃ q, p.getQuarter s1 = some q ∧ !q.isEmpty = true) → p.get...
L216   theorem    Shape.obstacle_all_grounded : (s : Shape) (allFallingPos : List QuarterPos) (h_perm : List.Perm allFallingPos ((Gravity.floatingUnits s).flatMap Gravity.FallingUnit.positions)) : ∀ (p : QuarterPos)...
L299   theorem    Shape.placeQuarter_length_ge : (obs : List Layer) (l : Nat) (d : Direction) (q : Quarter) : (Gravity.placeQuarter obs l d q).length ≥ obs.length
L311   lemma      Shape.isOccupied_false_isEmpty : (obs : List Layer) (layer : Nat) (dir : Direction) (h : Gravity.isOccupied obs layer dir = false) : (QuarterPos.getDir (obs.getD layer Layer.empty) dir).isEmpty = true
L331   theorem    Shape.placeFallingUnit_length_ge : (s : Shape) (obs : List Layer) (u : Gravity.FallingUnit) (d : Nat) : (Gravity.placeFallingUnit s obs u d).length ≥ obs.length
```
