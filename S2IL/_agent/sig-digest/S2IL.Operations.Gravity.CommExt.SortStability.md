# S2IL/Operations/Gravity/CommExt/SortStability.lean

- Lines: 792
- Declarations: 30 (theorems/lemmas: 29, defs/abbrev: 1, private: 8, sorry: 0)

## Landmarks

L6     namespace Gravity
L638   header    ## Length preservation of placeFallingUnit / placeLDGroups

## Declarations

```
L15    def        Gravity.foldMinOption : : Option Nat → Nat → Option Nat
L19    theorem    Gravity.foldMinOption_some_mem : (rest : List Nat) (init v : Nat) (h : rest.foldl foldMinOption (some init) = some v) : v = init ∨ v ∈ rest
L34    theorem    Gravity.foldMinOption_none_mem : (layers : List Nat) (v : Nat) (h : layers.foldl foldMinOption none = some v) : v ∈ layers
L46    theorem    Gravity.foldMinOption_some_isSome : (rest : List Nat) (init : Nat) : ∃ v, rest.foldl foldMinOption (some init) = some v
L53    theorem    Gravity.foldMinOption_some_le_init : (rest : List Nat) (init v : Nat) (h : rest.foldl foldMinOption (some init) = some v) : v ≤ init
L65    theorem    Gravity.foldMinOption_some_le_mem : (rest : List Nat) (init v : Nat) (h : rest.foldl foldMinOption (some init) = some v) (m : Nat) (hm : m ∈ rest) : v ≤ m
L80    theorem    Gravity.foldMinOption_none_le : (layers : List Nat) (v : Nat) (h : layers.foldl foldMinOption none = some v) (m : Nat) (hm : m ∈ layers) : v ≤ m
L92    theorem    Gravity.minLayerAtDir_some_witness : (u : FallingUnit) (dir : Direction) (l : Nat) (h : u.minLayerAtDir dir = some l) : ∃ p, p ∈ u.positions ∧ p.dir = dir ∧ p.layer = l
L107   theorem    Gravity.minLayerAtDir_some_of_mem : (u : FallingUnit) (dir : Direction) (p : QuarterPos) (hp : p ∈ u.positions) (hd : p.dir = dir) : ∃ l, u.minLayerAtDir dir = some l
L134   theorem    Gravity.shouldProcessBefore_false_minLayerAtDir_ge : (u v : FallingUnit) (dir : Direction) (h : shouldProcessBefore u v = false) (lu lv : Nat) (hu : u.minLayerAtDir dir = some lu) (hv : v.minLayerAtD...
L148   theorem    Gravity.tied_no_shared_dir : (u v : FallingUnit) (h_tied_uv : shouldProcessBefore u v = false) (h_tied_vu : shouldProcessBefore v u = false) (h_disj : ∀ p, p ∈ u.positions → v.positions.any (· == p) ...
L189   theorem    Gravity.tied_no_shared_dir_rev : (u v : FallingUnit) (h_tied_uv : shouldProcessBefore u v = false) (h_tied_vu : shouldProcessBefore v u = false) (h_disj : ∀ p, p ∈ v.positions → u.positions.any (· ==...
L197   theorem    Gravity.foldl_settle_head_swap : (s : Shape) (u v : FallingUnit) (rest : List FallingUnit) (obs : List Layer) (h_uv : ∀ p, p ∈ u.positions → v.positions.any (fun q => q.dir == p.dir) = false) (h_vu :...
L212   theorem    Gravity.minLayerAtDir_ext : {u1 u2 : FallingUnit} (h : ∀ p, u1.positions.any (· == p) = u2.positions.any (· == p)) (dir : Direction) : u1.minLayerAtDir dir = u2.minLayerAtDir dir
L278   theorem    Gravity.shouldProcessBefore_ext : {u1 u2 v1 v2 : FallingUnit} (hu : ∀ p, u1.positions.any (· == p) = u2.positions.any (· == p)) (hv : ∀ p, v1.positions.any (· == p) = v2.positions.any (· == p)) : sho...
L292   theorem    Gravity.insertSorted_length : (u : FallingUnit) (sorted : List FallingUnit) (fuel : Nat) : (insertSorted u sorted fuel).length = sorted.length + 1
L306   theorem    Gravity.sortFallingUnits_length : (units : List FallingUnit) : (sortFallingUnits units).length = units.length
L312   theorem    Gravity.insertSorted_pointwise_ext : {u1 u2 : FallingUnit} (hu : ∀ p, u1.positions.any (· == p) = u2.positions.any (· == p)) {sorted1 sorted2 : List FallingUnit} (h_len : sorted1.length = sorted2.len...
L412   theorem    Gravity.sortFallingUnits_pointwise_ext : {l1 l2 : List FallingUnit} (h_len : l1.length = l2.length) (h_pw : ∀ (i : Nat) (hi : i < l1.length) (p : QuarterPos), (l1[i]'hi).positions.any (· == p) = (l2[...
L486   theorem    Gravity.takeWhile_ld_length_eq[priv] : (l1 l2 : List FallingUnit) (obs : List Layer) (d : Nat) (h_len : l1.length = l2.length) (h_any : ∀ (i : Nat) (hi : i < l1.length) (p : QuarterPos), (l1[i]'hi)...
L507   theorem    Gravity.dropWhile_ld_length_eq[priv] : (l1 l2 : List FallingUnit) (obs : List Layer) (d : Nat) (h_len : l1.length = l2.length) (h_any : ∀ (i : Nat) (hi : i < l1.length) (p : QuarterPos), (l1[i]'hi)...
L528   theorem    Gravity.foldl_pfu_fixed_d_pointwise_ext[priv] : (s : Shape) (l1 l2 : List FallingUnit) (obs : List Layer) (d : Nat) (h_len : l1.length = l2.length) (h_any : ∀ (i : Nat) (hi : i < l1.length) (p : Qu...
L552   theorem    Gravity.placeLDGroups_ext : (s : Shape) (l1 l2 : List FallingUnit) (obs : List Layer) (h_len : l1.length = l2.length) (h_any : ∀ (i : Nat) (hi : i < l1.length) (p : QuarterPos), (l1[i]'hi).positions....
L642   theorem    Gravity.placeFallingUnit_length[priv] : (s : Shape) (obs : List Layer) (u : FallingUnit) (d : Nat) (h_d : ∀ p ∈ u.positions, p.layer < obs.length) (h_le : ∀ p ∈ u.positions, d ≤ p.layer) : (placeFa...
L666   theorem    Gravity.foldl_placeFallingUnit_length[priv] : (s : Shape) (d : Nat) (fus : List FallingUnit) (obs : List Layer) (h_d : ∀ u ∈ fus, ∀ p ∈ u.positions, p.layer < obs.length) (h_le : ∀ u ∈ fus, ∀ p ∈ u...
L686   theorem    Gravity.mem_takeWhile_pred[priv] : {α : Type*} (p : α → Bool) (l : List α) (x : α) (h : x ∈ l.takeWhile p) : p x = true
L699   theorem    Gravity.placeLDGroups_length : (s : Shape) (sorted : List FallingUnit) (obs : List Layer) (h_lt : ∀ u ∈ sorted, ∀ p ∈ u.positions, p.layer < obs.length) (h_le : ∀ u ∈ sorted, ∀ p ∈ u.positions, landi...
L723   theorem    Gravity.landing_sum_eq_gls[priv] : (group : List FallingUnit) (d : Nat) : (List.map (fun ld => ld.1 + 1) (List.flatMap (fun u => List.map (fun p => (p.layer - d, p.dir)) u.positions) group)).sum = ...
L729   theorem    Gravity.mem_tw_sat[priv] : {α : Type*} (P : α → Bool) (l : List α) (a : α) (h : a ∈ l.takeWhile P) : P a = true
L739   theorem    Gravity.placeLDGroupsLandings_sum_succ_le : (s : Shape) (sorted : List FallingUnit) (obs : List Layer) (h_ne : sorted ≠ []) (h_ml : ∀ u ∈ sorted, u.minLayer ≥ 1) (h_pne : ∀ u ∈ sorted, u.positions ≠ ...
```
