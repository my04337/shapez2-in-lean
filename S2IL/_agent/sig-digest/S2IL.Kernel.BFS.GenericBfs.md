# S2IL/Kernel/BFS/GenericBfs.lean

- Lines: 649
- Declarations: 29 (theorems/lemmas: 26, defs/abbrev: 2, private: 7, sorry: 0)

## Landmarks

L4     header    # 汎用 BFS 完全性

## Declarations

```
L12    def        genericBfs : [BEq α] (edge : α → α → Bool) (allNodes visited queue : List α) : (fuel : Nat) → List α | 0 => visited | fuel + 1 => match queue with | [] => visited | pos :: rest => if visited.any ...
L32    inductive  GenericReachable : (edge : α → α → Bool) : α → α → Prop where
L37    theorem    GenericReachable.trans : {edge : α → α → Bool} (h1 : GenericReachable edge p q) (h2 : GenericReachable edge q r) : GenericReachable edge p r
L45    theorem    GenericReachable.symm : {edge : α → α → Bool} (h_symm : ∀ a b, edge a b = edge b a) (h : GenericReachable edge p q) : GenericReachable edge q p
L55    theorem    GenericReachable.mono : {edge1 edge2 : α → α → Bool} (h_imp : ∀ a b, edge1 a b = true → edge2 a b = true) (hr : GenericReachable edge1 seed p) : GenericReachable edge2 seed p
L64    theorem    GenericReachable.map_step : {edge1 : α → α → Bool} {edge2 : β → β → Bool} {f : α → β} (h_step : ∀ a b, edge1 a b = true → edge2 (f a) (f b) = true) {x y : α} (h_reach : GenericReachable edge1...
L75    theorem    GenericReachable.map_step_inv : {edge1 : α → α → Bool} {edge2 : β → β → Bool} {f : α → β} {P : α → Prop} (h_step : ∀ a b, P a → edge1 a b = true → P b ∧ edge2 (f a) (f b) = true) {x y : α} (h...
L92    theorem    GenericReachable.non_bridge_removal : {edge1 edge2 : α → α → Bool} (h_reach : GenericReachable edge1 s x) (h_cycle : ∀ a b, edge1 a b = true → edge2 a b = false → GenericReachable edge2 a b) ...
L108   theorem    GenericReachable.edge_preserve_of_reachable : {edge1 edge2 : α → α → Bool} {s x : α} (h_reach : GenericReachable edge1 s x) (h : ∀ p q, GenericReachable edge1 s p → edge1 p q = true → edge2 p...
L128   theorem    genericBfs_edge_congr : [BEq α] [LawfulBEq α] (edge1 edge2 : α → α → Bool) (h_eq : ∀ a b, edge1 a b = edge2 a b) (allNodes vis queue : List α) (fuel : Nat) : genericBfs edge1 allNodes vis que...
L154   theorem    genericBfs_vis_subset : [BEq α] (edge : α → α → Bool) (allNodes vis queue : List α) (fuel : Nat) (p : α) : vis.any (· == p) = true → (genericBfs edge allNodes vis queue fuel).any (· == p) = true
L171   theorem    genericBfs_contains_start : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes : List α) (start : α) (fuel : Nat) (h_fuel : fuel > 0) : (genericBfs edge allNodes [] [start] fuel).any (· ==...
L183   theorem    genericBfs_sound : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis queue : List α) (fuel : Nat) (p : α) : (genericBfs edge allNodes vis queue fuel).any (· == p) = true → vis.any (· ...
L231   def        GenericBfsInv : [BEq α] (edge : α → α → Bool) (allNodes vis queue : List α) : Prop
L240   theorem    GenericBfsInv_initial : [BEq α] (edge : α → α → Bool) (allNodes : List α) (start : α) : GenericBfsInv edge allNodes [] [start]
L246   theorem    GenericBfsInv_skip : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis : List α) (pos : α) (rest : List α) (h_inv : GenericBfsInv edge allNodes vis (pos :: rest)) (h_vis : vis.any (· ...
L262   theorem    GenericBfsInv_process : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis : List α) (pos : α) (rest : List α) (h_inv : GenericBfsInv edge allNodes vis (pos :: rest)) (_h_not_vis : ¬(v...
L300   theorem    filter_length_lt_of_mem_of_not'[priv] : [BEq α] (l : List α) (pred : α → Bool) (x : α) (h_mem : x ∈ l) (h_not : pred x = false) : (l.filter pred).length < l.length
L313   theorem    filter_and_length_le'[priv] : [BEq α] (l : List α) (p q : α → Bool) : (l.filter fun x => p x && q x).length ≤ (l.filter q).length
L322   theorem    add_sq_le_sq_of_lt'[priv] : (nb u' u : Nat) (h_nb : nb ≤ u') (h_lt : u' < u) : nb + u' * u' ≤ u * u
L331   theorem    fuel_key_estimate[priv] : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis : List α) (pos : α) (h_nv : ¬(vis.any (· == pos) = true)) (h_edge_valid : ∀ p q, edge p q = true → allNod...
L401   theorem    genericBfs_invariant_preserved : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis queue : List α) (fuel : Nat) (h_inv : GenericBfsInv edge allNodes vis queue) (h_fuel : fuel + 1 ≥ qu...
L459   theorem    genericBfs_queue_in_result : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis queue : List α) (fuel : Nat) (p : α) (h_mem : vis.any (· == p) = true ∨ queue.any (· == p) = true) (h_in...
L546   theorem    genericBfs_closed_contains_reachable : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis : List α) (h_closed : GenericBfsInv edge allNodes vis []) (start p : α) (h_start : vis.any (· ...
L565   theorem    any_map_eq[priv] : [BEq α] (σ : α → α) (h_beq : ∀ a b, (σ a == σ b) = (a == b)) (l : List α) (p : α) : (l.map σ).any (· == σ p) = l.any (· == p)
L571   theorem    any_cons_map_eq[priv] : [BEq α] (σ : α → α) (h_beq : ∀ a b, (σ a == σ b) = (a == b)) (x : α) (l : List α) (p : α) : (σ x :: l.map σ).any (· == σ p) = (x :: l).any (· == p)
L577   theorem    filter_map_comm[priv] : [BEq α] (edge edge' : α → α → Bool) (σ : α → α) (h_edge : ∀ a b, edge' (σ a) (σ b) = edge a b) (h_beq : ∀ a b, (σ a == σ b) = (a == b)) (allNodes : List α) (pos : α)...
L598   theorem    genericBfs_map_comm : [BEq α] [LawfulBEq α] (edge edge' : α → α → Bool) (σ : α → α) (h_edge : ∀ a b, edge' (σ a) (σ b) = edge a b) (h_beq : ∀ a b, (σ a == σ b) = (a == b)) (allNodes vis queue...
L629   theorem    genericBfs_nodup : [BEq α] [LawfulBEq α] (edge : α → α → Bool) (allNodes vis queue : List α) (fuel : Nat) (h_vis : vis.Nodup) : (genericBfs edge allNodes vis queue fuel).Nodup
```
